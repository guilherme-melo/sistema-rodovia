syntax = 'proto3';

package roadsim;

// lets create functions for the road service
service RoadSim {
  rpc Simulate (Request) returns (Response);
};

message Request {
  string name = 1;
};

message Response {
  string message = 1;
};

